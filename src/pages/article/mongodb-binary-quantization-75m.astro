---
import Layout from "../../layouts/Layout.astro";
import LatencyChart from "../../components/LatencyChart";
import QuantizationCharts from "../../components/QuantizationCharts";
import SearchMechanism from "../../components/SearchMechanism";

const title = "MongoDB Vector Search Binary Quantization at 75 Million Vectors — Justine Winata";
const description = "A MongoDB Vector Search benchmark measuring binary and scalar quantization recall and isolated binary-search latency at 75-million scale.";
const sourceDistribution = [
  { network: "ARDOT · Arkansas", documents: "30,876,071", share: "41.2%", cameras: "522" },
  { network: "Caltrans · California", documents: "16,834,275", share: "22.4%", cameras: "318" },
  { network: "DelDOT · Delaware", documents: "11,675,979", share: "15.6%", cameras: "377" },
  { network: "Bison Futé · France", documents: "3,992,793", share: "5.3%", cameras: "150" },
  { network: "Weacom · Russia", documents: "3,223,126", share: "4.3%", cameras: "81" },
  { network: "Smartech LATAM · Peru", documents: "2,634,624", share: "3.5%", cameras: "31" },
  { network: "ITIC · Thailand", documents: "2,215,975", share: "3.0%", cameras: "44" },
  { network: "KazToll · Kazakhstan", documents: "1,056,016", share: "1.4%", cameras: "190" },
  { network: "SHA · Maryland", documents: "948,796", share: "1.3%", cameras: "28" },
  { network: "KT · Kyrgyzstan", documents: "939,486", share: "1.3%", cameras: "29" },
  { network: "Remaining hosts", documents: "603,508", share: "0.8%", cameras: "—" },
];
---

<Layout title={title} description={description} socialImage="/writing/mongodb-vector-search-binary-quantization-75m.png" type="article">
  <main class="article-page">
    <div class="reading-progress" aria-hidden="true"></div>
    <article>
      <header class="article-hero">
        <div class="article-hero-copy">
          <a class="article-back" href="/">← Justine Winata</a>
          <p class="eyebrow">MongoDB Vector Search · benchmark · August 2026</p>
          <h1>Does MongoDB Vector Search’s binary quantization hold at 75 million vectors?</h1>
          <p class="article-deck">MongoDB’s public Vector Search benchmark reaches 15.3 million vectors. I wanted to know what happens much further out—when MongoDB Vector Search runs approximate search over 75 million real-world embeddings but can rescore at most 10,000 candidates.</p>
        </div>
        <div class="article-meta" aria-label="Benchmark summary">
          <div><span>corpus</span><strong>75,000,649</strong></div>
          <div><span>dimensions</span><strong>512</strong></div>
          <div><span>queries</span><strong>300</strong></div>
          <div><span>metric</span><strong>recall@72</strong></div>
          <div><span>reading time</span><strong>17 min</strong></div>
        </div>
        <details class="mobile-toc">
          <summary>On this page</summary>
          <nav aria-label="Article sections">
            <a href="#gap">The benchmark gap</a>
            <a href="#mechanism">How MongoDB quantization fails</a>
            <a href="#data">Getting the data</a>
            <a href="#method">Method and ground truth</a>
            <a href="#result-controls">Interactive results</a>
            <a href="#reading">Reading the result</a>
            <a href="#latency">Latency benchmark</a>
            <a href="#ceiling">The candidate ceiling</a>
            <a href="#storage">Resource tradeoff</a>
            <a href="#limitations">Limitations</a>
          </nav>
        </details>
      </header>

      <div class="article-prose">
        <aside class="headline-result">
          <div><strong>95.8%</strong><span>MongoDB binary recall · 10,000 candidates</span></div>
          <p><strong>MongoDB Vector Search’s candidate ceiling did not cause binary recall to collapse on this corpus.</strong> Its binary index kept improving across the entire measured range. The scalar index plateaued near 73%, and additional search effort barely moved it.</p>
        </aside>

        <section id="gap">
          <p class="section-label">01 · The benchmark gap</p>
          <h2>Fifteen million vectors does not answer the 75-million-vector question</h2>
          <p>MongoDB’s large public Vector Search benchmark uses 15.3 million, 2048-dimensional embeddings and reports 90–95% recall with quantization. That is useful evidence, but it is a different regime from 75 million vectors at 512 dimensions.</p>
          <p>The part that bothered me was the fixed search budget. MongoDB Vector Search caps <code>numCandidates</code> at 10,000. The corpus can keep growing; the pool eligible for exact rescoring cannot. If true neighbours stop reaching that pool, no reranking stage can recover them.</p>
          <p class="question-callout">At what scale does a fixed 10,000-candidate budget stop being enough—and had 75 million vectors already crossed that line?</p>
        </section>

        <section id="mechanism">
          <p class="section-label">02 · Mechanism</p>
          <h2>MongoDB Vector Search’s binary and scalar indexes fail in different places</h2>
          <p>Both MongoDB Vector Search indexes use compressed values during approximate nearest-neighbour search. The difference comes after retrieval.</p>
          <SearchMechanism client:load />
          <p>MongoDB’s binary path only has to retrieve a true neighbour into the candidate pool. MongoDB Vector Search then uses the retained full-precision vectors to repair its ordering. Its scalar path ranks on the approximation, so increasing the pool cannot necessarily buy back information already lost in that comparison.</p>
        </section>

        <section id="data">
          <p class="section-label">03 · Getting the data</p>
          <h2>Seventy-five million real embeddings from public road-camera feeds</h2>
          <p>The corpus came from public traffic-camera streams available online. It combined open HLS feeds from state transport agencies with a public catalogue of HD road cameras. Across the run, 2,126 cameras on 23 hosts contributed footage from at least nine countries.</p>
          <p>Each stream was decoded at one frame per second. YOLO detected the vehicles in each frame, the detections were cropped, and an embedding model converted every crop into a 512-dimensional vector. Capture, detection, and embedding ran as separate stages across two hosts. Sharding camera groups over four capture processes lifted the pipeline from 5.5 to a peak of 1,104 documents per second.</p>
          <figure class="process-flow process-flow-data" aria-labelledby="data-pipeline-caption">
            <figcaption id="data-pipeline-caption"><span>Collection pipeline</span><strong>From public video feeds to searchable vectors</strong></figcaption>
            <div class="process-flow-track process-flow-track-5" role="list">
              <div class="process-step" role="listitem"><span>01</span><div><strong>Public HLS streams</strong><small>2,126 cameras · 23 hosts</small></div></div>
              <b class="process-connector" aria-hidden="true">→</b>
              <div class="process-step" role="listitem"><span>02</span><div><strong>Decode frames</strong><small>Sample each stream at 1 fps</small></div></div>
              <b class="process-connector" aria-hidden="true">→</b>
              <div class="process-step" role="listitem"><span>03</span><div><strong>Detect & crop</strong><small>YOLO isolates each vehicle</small></div></div>
              <b class="process-connector" aria-hidden="true">→</b>
              <div class="process-step" role="listitem"><span>04</span><div><strong>Create embedding</strong><small>Convert crops into 512 dimensions</small></div></div>
              <b class="process-connector" aria-hidden="true">→</b>
              <div class="process-step" role="listitem"><span>05</span><div><strong>Store in MongoDB</strong><small>75,000,649 searchable documents</small></div></div>
            </div>
          </figure>
          <div class="corpus-table-wrap">
            <table>
              <caption>Corpus distribution by camera network</caption>
              <thead><tr><th>network</th><th>documents</th><th>share</th><th>cameras</th></tr></thead>
              <tbody>
                {sourceDistribution.map((source) => <tr><td>{source.network}</td><td>{source.documents}</td><td>{source.share}</td><td>{source.cameras}</td></tr>)}
              </tbody>
              <tfoot><tr><th>total</th><th>75,000,649</th><th>100%</th><th>2,126</th></tr></tfoot>
            </table>
          </div>
          <p>The collection ran for about 46 hours and produced roughly 260 GB of documents. Each record contained a normalized vehicle bounding box and a 512-dimensional embedding stored as BSON binary data.</p>
        </section>

        <section id="method">
          <p class="section-label">04 · Method</p>
          <h2>Exact ground truth without 300 exhaustive MongoDB searches</h2>
          <p>The benchmark measures MongoDB Vector Search over 75 million real-world visual embeddings, anonymized here to keep the experiment independent from any product or organisation. I selected 300 fixed query vectors and stripped each query’s self-match from both the exact and approximate result sets.</p>
          <pre><code>recall@k = | top-k(approximate) ∩ top-k(exact) | / k</code></pre>
          <p>Running an exact database search once per query was impractical at this scale. Instead, the corpus was streamed once in chunks. Every chunk was scored against all queries with exact float32 cosine similarity, while each query maintained a running top-200 pool.</p>
          <ul>
            <li><strong>Arms:</strong> MongoDB Vector Search indexes using binary and scalar quantization.</li>
            <li><strong>Candidate sweep:</strong> 360, 720, 1080, 1440, 4000, and 10000.</li>
            <li><strong>Primary metric:</strong> document-ID overlap at k=72—not the score reported by the index.</li>
            <li><strong>Tail metrics:</strong> median, p5, p95, and per-query perfect recall.</li>
          </ul>
          <p>The exact pool was validated independently by rereading stored vectors and recomputing their cosine values. Agreement was within float32 precision.</p>
        </section>
      </div>

      <section class="article-results" id="results">
        <div class="article-prose results-intro">
          <p class="section-label">05 · Results</p>
          <h2>More candidates rescue MongoDB binary search. They barely affect scalar.</h2>
          <p>The controls below are linked across every chart. Select one candidate setting and the mean recall and query-tail readouts update together.</p>
        </div>
        <div id="result-controls" class="results-controls-anchor">
          <QuantizationCharts client:load />
        </div>
      </section>

      <div class="article-prose">
        <section id="reading">
          <p class="section-label">06 · Reading the result</p>
          <h2>The mean is only half the finding</h2>
          <p>At 360 candidates, scalar begins 1.4 recall points ahead. By 720, binary has crossed it. At 10,000 candidates, binary reaches 95.8% mean recall while scalar remains at 73.1%.</p>
          <p>The tail is more revealing. Binary’s fifth percentile rises from 40.3% to 81.9%. Scalar’s fifth percentile is exactly 19.4% at every setting. The least successful scalar queries recover the same small fraction whether the search receives 360 candidates or 10,000.</p>
          <blockquote>A floor that more search cannot lift points to information lost during ranking, not neighbours that merely need more exploration.</blockquote>
        </section>

        <section id="latency">
          <p class="section-label">07 · Latency benchmark</p>
          <h2>Latency Benchmark</h2>
          <p>The recall experiment was designed to compare correctness, not to produce a clean latency number. Binary, scalar-quantized, and unquantized index builds shared the environment while evaluation scripts generated additional CPU, memory, and I/O work. Timing those queries would have measured the research harness as much as MongoDB Vector Search.</p>
          <p>I therefore ran latency as a separate experiment over the same 75-million-document collection. I isolated one 512-dimensional binary index, cleared the host page cache, and then accessed only that index so unrelated vector indexes would not occupy memory. This removed the cross-index cache eviction present in the earlier test setup.</p>

          <figure class="process-flow process-flow-latency" aria-labelledby="latency-pipeline-caption">
            <figcaption id="latency-pipeline-caption"><span>Isolation protocol</span><strong>How the latency run removed competing work</strong></figcaption>
            <div class="process-flow-track process-flow-track-4" role="list">
              <div class="process-step" role="listitem"><span>01</span><div><strong>Stop other jobs</strong><small>Pause recall evaluation and index builds</small></div></div>
              <b class="process-connector" aria-hidden="true">→</b>
              <div class="process-step" role="listitem"><span>02</span><div><strong>Isolate one index</strong><small>Keep only the binary index active</small></div></div>
              <b class="process-connector" aria-hidden="true">→</b>
              <div class="process-step" role="listitem"><span>03</span><div><strong>Warm the system</strong><small>Bound mongod and warm mongot</small></div></div>
              <b class="process-connector" aria-hidden="true">→</b>
              <div class="process-step" role="listitem"><span>04</span><div><strong>Measure new queries</strong><small>1,300 unseen-query observations</small></div></div>
            </div>
          </figure>

          <p>The sweep covered 13 <code>numCandidates</code> settings from 100 to 10,000. At each setting, I ran 100 warm queries using vectors from previously unseen documents—1,300 measured queries in total. Every query passed its stored 512-dimensional BinData vector directly as <code>queryVector</code>, with no decoding, no pre-filter, and <code>limit: 72</code>.</p>
          <p>The latency host had 62.7 GB of RAM, 16 cores, and no swap. Its working configuration capped the <code>mongod</code> container at 32 GB, reduced the WiredTiger cache to 8 GB, and left <code>mongot</code> uncapped.</p>

          <div class="latency-results-surface">
            <div class="latency-results-inner">
              <LatencyChart client:load />

              <div class="latency-table-wrap latency-results">
                <table>
                  <caption>Full 13-setting latency sweep</caption>
                  <thead><tr><th>numCandidates</th><th>p50</th><th>p95</th><th>p99</th><th>max</th></tr></thead>
                  <tbody>
                    <tr><td>100</td><td>46 ms</td><td>63 ms</td><td>82 ms</td><td>82 ms</td></tr>
                    <tr><td>360</td><td>176 ms</td><td>210 ms</td><td>268 ms</td><td>268 ms</td></tr>
                    <tr><td>720</td><td>198 ms</td><td>265 ms</td><td>730 ms</td><td>730 ms</td></tr>
                    <tr><td>1,000</td><td>201 ms</td><td>229 ms</td><td>375 ms</td><td>375 ms</td></tr>
                    <tr><td>2,000</td><td>255 ms</td><td>278 ms</td><td>387 ms</td><td>387 ms</td></tr>
                    <tr><td>3,000</td><td>299 ms</td><td>322 ms</td><td>400 ms</td><td>400 ms</td></tr>
                    <tr><td>4,000</td><td>346 ms</td><td>373 ms</td><td>407 ms</td><td>407 ms</td></tr>
                    <tr><td>5,000</td><td>397 ms</td><td>428 ms</td><td>981 ms</td><td>981 ms</td></tr>
                    <tr><td>6,000</td><td>446 ms</td><td>477 ms</td><td>1,082 ms</td><td>1,082 ms</td></tr>
                    <tr><td>7,000</td><td>494 ms</td><td>530 ms</td><td>723 ms</td><td>723 ms</td></tr>
                    <tr><td>8,000</td><td>537 ms</td><td>575 ms</td><td>608 ms</td><td>608 ms</td></tr>
                    <tr><td>9,000</td><td>591 ms</td><td>637 ms</td><td>984 ms</td><td>984 ms</td></tr>
                    <tr><td>10,000</td><td>635 ms</td><td>668 ms</td><td>945 ms</td><td>945 ms</td></tr>
                  </tbody>
                </table>
              </div>
            </div>
          </div>

          <p><strong>None of the 1,300 measured queries exceeded two seconds.</strong> At MongoDB’s 10,000-candidate ceiling, median latency was 635 ms—7.9× under that budget—and p95 was 668 ms. The worst observation in the full sweep was 1,082 ms at 6,000 candidates.</p>
          <p>From 1,000 to 10,000 candidates, the median increased by 434 ms. A linear fit over that range adds 0.0481 ms per candidate, with no high-candidate inflection point.</p>
          <p>This is a warm steady-state result, not a cold-start promise. It applies when the index is isolated, the memory allocation matches this setup, and the working set has been warmed.</p>
          <blockquote>The recall curve says a wider candidate pool buys accuracy. The isolated latency run says that, once warm, the same lever remained affordable on this host.</blockquote>
        </section>

        <section id="ceiling">
          <p class="section-label">08 · The 10,000-candidate ceiling</p>
          <h2>So? Can MongoDB binary quantization still be trusted?</h2>
          <p><strong>It depends. For this corpus and this query set, yes—at a measured operating point.</strong> The recall curve is still rising at 10,000 candidates, and its tail improves alongside its mean. There is no sign of a catastrophic ceiling at 75 million vectors.</p>
          <p>The more general answer depends less on document count than on the geometry of the embedding space. By “dense,” I mean a neighbourhood with many vectors separated by very small similarity margins. In that setting, the distortion introduced by a 1-bit comparison is more likely to change which edges the ANN traversal follows. A true neighbour that never enters the candidate pool cannot be recovered by exact rescoring.</p>
          <figure class="density-flow">
            <figcaption>How local vector density changes the risk of binary quantization</figcaption>
            <div class="density-case density-case-separated">
              <div class="density-step">
                <span>neighbourhood</span>
                <strong>Well separated</strong>
              </div>
              <b class="density-arrow" aria-hidden="true">→</b>
              <div class="density-step">
                <span>ANN traversal</span>
                <p>Distortion stays small relative to the similarity margins.</p>
              </div>
              <b class="density-arrow" aria-hidden="true">→</b>
              <div class="density-step density-outcome">
                <span>outcome</span>
                <p>True neighbours are likely to reach the candidate pool.</p>
              </div>
            </div>
            <div class="density-case density-case-crowded">
              <div class="density-step">
                <span>neighbourhood</span>
                <strong>Dense · many near-ties</strong>
              </div>
              <b class="density-arrow" aria-hidden="true">→</b>
              <div class="density-step">
                <span>ANN traversal</span>
                <p>The 1-bit comparison can erase fine distinctions.</p>
              </div>
              <b class="density-arrow" aria-hidden="true">→</b>
              <div class="density-step density-outcome">
                <span>outcome</span>
                <p>True neighbours can be lost before exact rescoring.</p>
              </div>
            </div>
          </figure>
          <p>I have tested other, denser vector sets and observed lower binary recall. That does not contradict this result; it shows why trust has to be earned on the corpus being searched. When neighbourhoods are crowded, test a more accurate representation for ANN traversal as well as a wider candidate pool.</p>
        </section>

        <section id="storage">
          <p class="section-label">09 · Resource tradeoff</p>
          <h2>MongoDB binary quantization buys search memory, not equivalent disk savings</h2>
          <p>MongoDB Vector Search’s compact 1-bit representation reduces the vector working set used by approximate search. But its automated binary quantization retains full-precision vectors for rescoring. Storage planning must account for both representations and the graph rather than assuming a 32× smaller index on disk.</p>
        </section>

        <section id="limitations">
          <p class="section-label">10 · Limitations</p>
          <h2>What this benchmark does not prove</h2>
          <ul>
            <li><strong>One corpus is not a scaling law.</strong> The result does not isolate corpus size from embedding distribution.</li>
            <li><strong>No query-time filters.</strong> Selective filters can materially change graph exploration.</li>
            <li><strong>Geometric recall is not relevance.</strong> This measures nearest-neighbour fidelity, not whether a human prefers the result.</li>
          </ul>
        </section>

        <section class="references" aria-labelledby="references-title">
          <p class="section-label">References</p>
          <h2 id="references-title">Primary sources</h2>
          <ol>
            <li><a href="https://www.mongodb.com/docs/vector-search/benchmark/results/" target="_blank" rel="noopener noreferrer">MongoDB Vector Search Benchmark Results</a></li>
            <li><a href="https://www.mongodb.com/docs/vector-search/benchmark/overview/" target="_blank" rel="noopener noreferrer">MongoDB Vector Search Benchmark Overview</a></li>
            <li><a href="https://www.mongodb.com/docs/search/query/operators-collectors/vectorSearch/" target="_blank" rel="noopener noreferrer">MongoDB <code>vectorSearch</code> operator reference</a></li>
            <li><a href="https://www.mongodb.com/docs/vector-search/about/vector-quantization/" target="_blank" rel="noopener noreferrer">MongoDB Vector Quantization</a></li>
          </ol>
        </section>

        <footer class="article-footer">
          <p>Written by <a href="/">Justine Winata</a>, a senior software engineer working on AI infrastructure and distributed systems.</p>
          <a class="email-button" href="mailto:tintin6892@gmail.com">discuss this benchmark</a>
        </footer>
      </div>
    </article>
  </main>
</Layout>

<style is:global>
  .article-page {
    --article-reading-width: 680px;
    --article-wide-width: 680px;
    --article-inline-gutter: 48px;
    --article-visual-gutter: 64px;
    overflow: clip;
  }
  .article-page article { padding: 0 0 88px; }
  .reading-progress { position: fixed; z-index: 30; inset: 0 0 auto; height: 3px; background: var(--accent); transform: scaleX(0); transform-origin: left; animation: article-progress linear; animation-timeline: scroll(root); }
  @keyframes article-progress { to { transform: scaleX(1); } }
  .article-hero,
  .article-prose { width: min(var(--article-reading-width), calc(100% - var(--article-inline-gutter))); margin: 0 auto; }
  .article-hero { padding: 54px 0 0; }
  .article-back { display: inline-block; margin-bottom: 42px; color: var(--muted); font: 12px/1.5 var(--mono); text-decoration: none; }
  .article-back:hover { color: var(--accent); }
  .article-hero h1 { margin: 12px 0 20px; font-size: clamp(48px, 6.4vw, 60px); font-weight: 600; line-height: .98; letter-spacing: -.048em; }
  .article-deck { margin: 0; color: var(--body); font-size: 22px; line-height: 1.55; }
  .article-meta { display: grid; grid-template-columns: repeat(5, 1fr); margin-top: 42px; border: 1px solid var(--line); border-width: 1px 0; }
  .article-meta div { display: grid; gap: 4px; padding: 16px 18px; border-right: 1px solid var(--line-soft); }
  .article-meta div:first-child { padding-left: 0; }
  .article-meta div:last-child { border-right: 0; }
  .article-meta span,
  .section-label,
  .headline-result span,
  .article-meta strong { font: 500 14px/1.5 var(--mono); }
  .article-prose { color: var(--body); font-size: 19px; line-height: 1.72; }
  .article-prose > section { margin-top: 74px; scroll-margin-top: 24px; }
  .article-prose h2,
  .results-intro h2 { margin: 7px 0 20px; color: var(--ink); font-size: clamp(28px, 4vw, 38px); line-height: 1.16; letter-spacing: -.025em; }
  .article-prose p { margin: 18px 0; }
  .article-prose code { font: .84em/1.5 var(--mono); }
  .article-prose pre { margin: 25px 0; padding: 18px 20px; overflow-x: auto; background: #f3f0ea; border: 1px solid var(--line-soft); border-radius: 4px; color: var(--ink); }
  .article-prose ul { padding-left: 22px; }
  .article-prose li { margin: 7px 0; }
  .article-prose blockquote { margin: 30px 0; padding: 2px 0 2px 24px; border-left: 2px solid var(--accent); color: var(--ink); font-size: 23px; line-height: 1.45; }
  .mobile-toc { display: none; }
  .headline-result { display: grid; grid-template-columns: 240px minmax(0, 1fr); gap: 34px; align-items: center; width: 100%; margin: 54px 0 0; padding: 30px 0; border: 1px solid var(--line); border-width: 1px 0; }
  .headline-result > div { display: grid; }
  .headline-result > div strong { color: var(--accent); font: 500 clamp(48px, 7vw, 66px)/1 var(--mono); letter-spacing: -.06em; }
  .headline-result p { margin: 0; }
  .question-callout { padding: 20px 24px; background: #f5f1ea; border-left: 2px solid var(--accent); color: var(--ink); font-size: 21px; }
  .corpus-table-wrap { width: min(var(--article-wide-width), calc(100vw - var(--article-visual-gutter))); margin: 30px 0 30px 50%; overflow-x: auto; border: 1px solid var(--line); border-radius: 5px; transform: translateX(-50%); }
  .corpus-table-wrap table { width: 100%; border-collapse: collapse; font: 11px/1.5 var(--mono); }
  .corpus-table-wrap caption { padding: 13px 15px; border-bottom: 1px solid var(--line); color: var(--ink); text-align: left; }
  .corpus-table-wrap th,
  .corpus-table-wrap td { padding: 8px 12px; border-bottom: 1px solid var(--line-soft); text-align: right; white-space: nowrap; }
  .corpus-table-wrap th:first-child,
  .corpus-table-wrap td:first-child { text-align: left; }
  .corpus-table-wrap thead th { color: var(--muted); font-weight: 400; }
  .corpus-table-wrap tfoot th { border-bottom: 0; color: var(--ink); font-weight: 600; }
  .article-results { margin-top: 86px; padding: 76px 0 28px; background: linear-gradient(180deg, #f5f2ec 0, #faf8f4 100%); border: 1px solid var(--line-soft); border-width: 1px 0; scroll-margin-top: 24px; }
  .results-intro { margin-bottom: 28px; }
  .results-controls-anchor { scroll-margin-top: 18px; }
  .chart-suite { width: min(var(--article-wide-width), calc(100% - var(--article-visual-gutter))); margin: 0 auto; }
  .candidate-control { display: flex; justify-content: space-between; gap: 20px; align-items: center; padding: 12px 14px; background: #f3f0ea; border: 1px solid var(--line-soft); border-radius: 5px; }
  .candidate-control > span { font: 11px/1.5 var(--mono); color: var(--muted); }
  .candidate-control > div { display: flex; flex-wrap: wrap; gap: 6px; }
  .candidate-control button { padding: 7px 10px; border: 1px solid transparent; border-radius: 3px; background: transparent; color: var(--body); font: 11px/1.2 var(--mono); cursor: pointer; }
  .candidate-control button:hover { border-color: var(--accent-soft); }
  .candidate-control button.is-active { background: var(--ink); color: var(--paper); }
  .selected-result { display: grid; grid-template-columns: 165px 165px 1fr; gap: 24px; align-items: center; padding: 26px 0 18px; }
  .selected-result > div { display: grid; }
  .selected-result span { color: var(--muted); font: 10px/1.4 var(--mono); text-transform: uppercase; letter-spacing: .08em; }
  .selected-result strong { font-size: 32px; line-height: 1.15; }
  .selected-result > div:first-child strong { color: var(--accent); }
  .selected-result small { color: var(--muted); font: 11px/1.5 var(--mono); }
  .selected-result p { margin: 0; color: var(--body); font-size: 16px; line-height: 1.55; }
  .selected-result p strong { color: var(--ink); font-size: inherit; font-weight: 600; line-height: inherit; }
  .chart-card { margin: 22px 0 54px; padding: 28px 32px; border: 1px solid var(--line); border-radius: 6px; background: #fff; box-shadow: 0 14px 38px rgba(35, 32, 28, .045); }
  .chart-card figcaption { display: grid; gap: 4px; margin-bottom: 16px; }
  .chart-card figcaption span { color: var(--accent); font: 10px/1.4 var(--mono); letter-spacing: .1em; text-transform: uppercase; }
  .chart-card figcaption strong { font-size: 22px; line-height: 1.3; }
  .chart-card figcaption small { max-width: 680px; color: var(--muted); font-size: 14px; line-height: 1.5; }
  .chart-scroll { overflow-x: auto; }
  .chart-scroll svg { display: block; width: 100%; min-width: 620px; height: auto; }
  .chart-grid { stroke: #ebe7df; stroke-width: 1; }
  .chart-axis-label,
  .chart-x-title { fill: #847e75; font-family: var(--mono); font-size: 10px; }
  .chart-line { fill: none; stroke-width: 2.5; stroke-linecap: round; stroke-linejoin: round; }
  .chart-line-binary { stroke: var(--accent); }
  .chart-line-scalar { stroke: #4d5963; stroke-dasharray: 6 5; }
  .chart-dot { stroke-width: 2; transition: r .15s ease, stroke-width .15s ease; }
  .chart-dot-binary { fill: #fff; stroke: var(--accent); }
  .chart-dot-scalar { fill: #fff; stroke: #4d5963; }
  .chart-dot.is-selected { fill: var(--paper); stroke-width: 3.5; }
  .chart-selection { stroke: #c7c0b6; stroke-width: 1; stroke-dasharray: 3 5; }
  .chart-hit-area { fill: transparent; cursor: crosshair; }
  .chart-hit-area:focus { outline: none; stroke: var(--accent); stroke-width: 1.5; stroke-dasharray: 4 5; }
  .chart-tooltip { pointer-events: none; }
  .chart-tooltip rect { fill: var(--ink); stroke: rgba(255, 255, 255, .18); stroke-width: 1; filter: drop-shadow(0 5px 12px rgba(35, 32, 28, .16)); }
  .chart-tooltip text { fill: var(--paper); font: 10px/1 var(--mono); }
  .chart-tooltip .chart-tooltip-title { fill: var(--accent-soft); font-weight: 500; letter-spacing: .04em; }
  .chart-band { stroke: none; opacity: .1; }
  .chart-band-binary { fill: var(--accent); }
  .chart-band-scalar { fill: #4d5963; }
  .chart-legend,
  .tail-readout { display: flex; flex-wrap: wrap; gap: 8px 20px; padding-top: 12px; border-top: 1px solid var(--line-soft); color: var(--muted); font: 10px/1.5 var(--mono); }
  .legend-binary::before,
  .legend-scalar::before { content: ""; display: inline-block; width: 16px; height: 2px; margin: 0 7px 3px 0; background: var(--accent); }
  .legend-scalar::before { background: repeating-linear-gradient(to right, #4d5963 0 5px, transparent 5px 8px); }
  .tail-readout strong { color: var(--ink); }
  .latency-results-surface { width: 100vw; margin: 32px 0 46px 50%; padding: 44px 0; background: linear-gradient(180deg, #f5f2ec 0, #faf8f4 100%); border: 1px solid var(--line-soft); border-width: 1px 0; transform: translateX(-50%); }
  .latency-results-inner { width: min(var(--article-wide-width), calc(100% - var(--article-visual-gutter))); margin: 0 auto; }
  .latency-results-surface .latency-chart { width: 100%; margin: 0 0 18px; transform: none; }
  .latency-results-surface .latency-table-wrap { width: 100%; margin: 0; background: #fff; transform: none; }
  .latency-table-wrap { width: min(var(--article-wide-width), calc(100vw - var(--article-visual-gutter))); margin: 30px 0 30px 50%; overflow-x: auto; border: 1px solid var(--line); border-radius: 5px; transform: translateX(-50%); }
  .latency-table-wrap table { width: 100%; border-collapse: collapse; font: 11px/1.5 var(--mono); }
  .latency-table-wrap caption { padding: 13px 15px; border-bottom: 1px solid var(--line); color: var(--ink); text-align: left; }
  .latency-table-wrap th,
  .latency-table-wrap td { padding: 10px 12px; border-bottom: 1px solid var(--line-soft); text-align: left; vertical-align: top; }
  .latency-table-wrap tbody tr:last-child td { border-bottom: 0; }
  .latency-table-wrap th { color: var(--muted); font-weight: 400; }
  .latency-table-wrap td:first-child { color: var(--ink); font-weight: 600; white-space: nowrap; }
  .latency-results th,
  .latency-results td { text-align: right; white-space: nowrap; }
  .latency-results th:first-child,
  .latency-results td:first-child { text-align: left; }
  .latency-chart { width: min(var(--article-wide-width), calc(100vw - var(--article-visual-gutter))); margin: 32px 0 54px 50%; transform: translateX(-50%); }
  .chart-line-p50 { stroke: var(--accent); }
  .chart-line-p95 { stroke: #4d5963; stroke-dasharray: 6 5; }
  .latency-dot { stroke-width: 2; transition: r .15s ease, stroke-width .15s ease; }
  .latency-dot-p50 { fill: #fff; stroke: var(--accent); }
  .latency-dot-p95 { fill: #fff; stroke: #4d5963; }
  .latency-dot.is-selected { fill: var(--paper); stroke-width: 3.5; }
  .legend-p50::before,
  .legend-p95::before { content: ""; display: inline-block; width: 16px; height: 2px; margin: 0 7px 3px 0; background: var(--accent); }
  .legend-p95::before { background: repeating-linear-gradient(to right, #4d5963 0 5px, transparent 5px 8px); }
  .density-flow { width: min(var(--article-wide-width), calc(100vw - var(--article-visual-gutter))); margin: 30px 0 30px 50%; overflow: hidden; border: 1px solid var(--line); border-radius: 5px; background: #fff; transform: translateX(-50%); }
  .density-flow figcaption { padding: 14px 17px; border-bottom: 1px solid var(--line); color: var(--ink); font: 500 12px/1.5 var(--mono); }
  .density-case { display: grid; grid-template-columns: minmax(125px, .8fr) 18px 1fr 18px 1fr; gap: 9px; align-items: stretch; padding: 15px; }
  .density-case + .density-case { border-top: 1px solid var(--line); }
  .density-case-separated { background: #f6f8f4; }
  .density-case-crowded { background: #faf4f0; }
  .density-step { min-width: 0; padding: 13px 14px; border: 1px solid var(--line-soft); border-radius: 4px; background: rgba(255, 255, 255, .72); }
  .density-step span { display: block; margin-bottom: 5px; color: var(--muted); font: 9.5px/1.4 var(--mono); letter-spacing: .09em; text-transform: uppercase; }
  .density-step strong { display: block; color: var(--ink); font: 600 14px/1.4 var(--mono); }
  .density-step p { margin: 0; color: var(--body); font-size: 13px; line-height: 1.45; }
  .density-case-separated .density-outcome { border-color: #b8c8ae; }
  .density-case-crowded .density-outcome { border-color: #d6b7a4; }
  .density-arrow { align-self: center; color: var(--accent); font: 400 16px/1 var(--mono); text-align: center; }
  .references ol { padding-left: 22px; }
  .references a { color: var(--accent-dark); text-decoration-thickness: 1px; text-underline-offset: 3px; }
  .article-footer { align-items: center; margin-top: 74px; }
  .article-footer p { max-width: 500px; margin: 0; }

  @media (max-width: 1380px) {
    .mobile-toc { display: block; margin-top: 22px; border: 1px solid var(--line); border-radius: 5px; }
    .mobile-toc summary { padding: 13px 15px; color: var(--ink); font: 11px/1.5 var(--mono); cursor: pointer; }
    .mobile-toc nav { display: grid; grid-template-columns: 1fr 1fr; gap: 0 18px; padding: 2px 15px 14px; }
    .mobile-toc a { padding: 6px 0; color: var(--muted); border-bottom: 1px solid var(--line-soft); font: 11px/1.35 var(--mono); text-decoration: none; }
  }

  @media (max-width: 720px) {
    .article-page {
      --article-inline-gutter: 40px;
      --article-visual-gutter: 24px;
    }
    .article-page article { padding-bottom: 54px; }
    .article-hero { padding-top: 34px; }
    .article-back { margin-bottom: 32px; }
    .article-hero h1 { font-size: clamp(38px, 12vw, 53px); }
    .article-deck { font-size: 18px; }
    .article-meta { grid-template-columns: 1fr 1fr; }
    .article-meta div:nth-child(2) { border-right: 0; }
    .article-meta div:first-child,
    .article-meta div:nth-child(3),
    .article-meta div:nth-child(5) { padding-left: 0; }
    .article-meta div:nth-child(-n+2) { border-bottom: 1px solid var(--line-soft); }
    .article-meta div:nth-child(3),
    .article-meta div:nth-child(4) { border-bottom: 1px solid var(--line-soft); }
    .article-meta div:last-child { grid-column: 1 / -1; }
    .mobile-toc nav { grid-template-columns: 1fr; }
    .headline-result { grid-template-columns: 1fr; gap: 18px; }
    .density-case { grid-template-columns: 1fr; gap: 7px; }
    .density-arrow { transform: rotate(90deg); }
    .candidate-control { align-items: flex-start; flex-direction: column; }
    .selected-result { grid-template-columns: 1fr 1fr; }
    .selected-result p { grid-column: 1 / -1; }
    .latency-results-surface { padding: 28px 0; }
    .chart-suite { width: calc(100% - var(--article-inline-gutter)); }
    .chart-card { margin-bottom: 36px; padding: 18px 14px; box-shadow: none; }
    .article-results { padding-top: 60px; }
    .article-prose > section { margin-top: 62px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .reading-progress { display: none; }
  }
</style>
