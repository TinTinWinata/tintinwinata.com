---
import Layout from "../layouts/Layout.astro";
import ExperienceTimeline from "../components/ExperienceTimeline";
import OlderProjects from "../components/OlderProjects";
import { articles, awards, certifications, earlierProjects, experiences, projects, skillGroups } from "../data/portfolio";

const title = "Justine Winata — AI & Fullstack Engineer";
const description = "Singapore-based engineer building production AI systems, video infrastructure and useful software.";
---

<Layout title={title} description={description}>
  <main>
    <div class="reading-column">
      <header class="intro">
        <img src="/profile.png" width="326" height="387" alt="Portrait of Justine Winata" />
        <div>
          <p class="eyebrow">AI & Fullstack Engineer · Singapore</p>
          <h1>Justine Winata</h1>
          <p class="lede">I’m an engineer in Singapore who builds AI systems that have to survive contact with production. Mostly that means video: pipelines that don’t drop frames, retrieval that answers in seconds, and agents that do something useful when they’re right and fail quietly when they’re not.</p>
          <nav class="contact-links" aria-label="Contact and profiles">
            <a href="mailto:tintin6892@gmail.com">email</a>
            <a href="https://github.com/TinTinWinata">github</a>
            <a href="https://www.linkedin.com/in/tintinwinata">linkedin</a>
            <a href="https://medium.com/@tintinwinata">medium</a>
            <a href="/Justine-Winata.pdf" download>résumé</a>
          </nav>
        </div>
      </header>

      <section aria-labelledby="experience-heading">
        <h2 id="experience-heading">What I’ve been doing</h2>
        <ExperienceTimeline experiences={experiences} client:load />
      </section>

      <section aria-labelledby="projects-heading">
        <h2 id="projects-heading">Things I’ve built</h2>
        <div class="project-list">
          {projects.map((project) => (
            <article class="project">
              <div class="project-image-wrap">
                <img src={project.image} alt={project.imageAlt} loading="lazy" />
              </div>
              <div>
                <div class="project-heading">
                  <h3>{project.name}</h3>
                  <span>{project.dates}</span>
                </div>
                <p>{project.prose}</p>
                {project.siteUrl && <a class="inline-link" href={project.siteUrl}>{project.site} ↗</a>}
              </div>
            </article>
          ))}
        </div>
        <OlderProjects projects={earlierProjects} client:idle />
      </section>

      <section aria-labelledby="writing-heading">
        <h2 id="writing-heading">Writing</h2>
        <p>I write up what I learn at work on <a class="inline-link" href="https://medium.com/@tintinwinata">Medium</a>.</p>
        <div class="article-list">
          {articles.map((article) => (
            <a href={article.url}>
              <time>{article.date}</time>
              <span>{article.title}</span>
            </a>
          ))}
        </div>
      </section>

      <section aria-labelledby="recognition-heading">
        <h2 id="recognition-heading">Recognition</h2>
        <div class="award-list">
          {awards.map((award) => (
            <article>
              <strong>{award.place}</strong>
              <div>
                {award.url ? <a href={award.url}>{award.title} ↗</a> : <span>{award.title}</span>}
                <small>{award.org}</small>
              </div>
            </article>
          ))}
        </div>
      </section>

      <section aria-labelledby="qualifications-heading">
        <h2 id="qualifications-heading">Qualifications</h2>
        <div class="certificate-grid">
          {certifications.map((certificate) => (
            <a href={certificate.url}>
              <span class="certificate-mark" aria-hidden="true">{certificate.mark}</span>
              <span>
                <strong>{certificate.title}</strong>
                <small>{certificate.org} · {certificate.year}</small>
              </span>
            </a>
          ))}
        </div>
        <div class="skills-list">
          {skillGroups.map((group) => (
            <div>
              <h3>{group.label}</h3>
              <p>{group.value}</p>
            </div>
          ))}
        </div>
      </section>

      <footer>
        <div>
          <p class="footer-kicker">Open to the right problem.</p>
          <h2>Hiring for AI or fullstack?</h2>
          <p>Singapore · remote-friendly · +65 9352 6719</p>
        </div>
        <a class="email-button" href="mailto:tintin6892@gmail.com">tintin6892@gmail.com</a>
      </footer>
    </div>
  </main>
</Layout>
